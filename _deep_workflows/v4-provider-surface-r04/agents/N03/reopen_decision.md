# Reopen Decision

The reopen was justified and it changed the spec materially.

Result:

- the prior OpenRouter lane should not survive as-is
- the campaign should remain open for **one packaging run**
- that packaging run should update the final matrix memo and implementation-facing study spec to swap in the Qwen lane and add an appendix/watchlist policy for DeepSeek, GLM, Kimi, MiniMax, and Mercury

This does **not** reopen the whole paper. It only fixes the provider panel so it matches the intended argument.
