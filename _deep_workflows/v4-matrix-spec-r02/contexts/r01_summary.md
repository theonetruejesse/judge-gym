# R01 Summary

R01 produced a candidate V4 matrix shape with:

- `Gilardi` primary
- `Zheng` default comparator
- `Ziems` as a scoped but not fully locked secondary lane
- OpenAI primary plus Anthropic and OpenRouter controls
- raw headline evidence plus `l2_neutralized` as the default semantic alternate

The unresolved decisions carried into R02 are:

1. whether `Ziems` belongs in the headline matrix or should be pruned from wave 1
2. whether OpenRouter wave 1 makes a provider-surface claim or a broader multi-model claim
3. the target-specific extension table
