{
  "nodeId": "N02",
  "kind": "agent",
  "role": "code-localizer",
  "objective": "Localize the score_gen pre-batch execution path and identify where durable continuation state should exist so the attempt-recording segment can resume instead of replaying one large fanout.",
  "inputs": [
    "apps/engine-temporal/src/run/service.ts",
    "apps/engine-temporal/src/workflows.ts",
    "apps/engine-temporal/src/activities.ts",
    "apps/engine-convex/convex/packages/worker.ts",
    "packages/engine-settings/src/index.ts",
    "packages/engine-settings/src/process.ts"
  ],
  "dependencies": [],
  "effects": [
    "read_code",
    "read_state",
    "write_private_artifacts"
  ],
  "allowedTools": [
    "exec_command"
  ],
  "allowedScripts": [],
  "writeScope": "agents/N02/",
  "requiredOutputs": [
    "findings.json",
    "patch_candidates.json"
  ],
  "completionCriteria": [
    "Identify the exact preflight guard and timeout source for the failing score_gen segment.",
    "Identify the current persistence boundary before batch registration.",
    "Propose at least one resumable patch shape and one rejected patch shape."
  ]
}
