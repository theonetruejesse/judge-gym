{
  "mcpServers": {
    "convex": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "convex@latest",
        "mcp",
        "start",
        "--project-dir",
        "${workspaceFolder}/packages/engine",
        "--disable-tools",
        "envList,envGet,envSet,envRemove"
      ],
      "env": {
        "CONVEX_DEPLOYMENT": "dev:patient-wolverine-343"
      }
    }
  }
}
