# Refactor-everything lab.ts exposes evidence endpoints

**Confidence:** 0.68

**Sources:**
- refactor-everything:packages/engine/convex/lab.ts

**Summary:**
In the refactor-everything branch, `convex/lab.ts` defines evidence-related endpoints including `initEvidenceWindow`, `initEvidenceWindowAndCollect`, `collectEvidence`, `insertEvidenceBatch`, `listEvidenceWindows`, `listEvidenceByWindow`, and `getEvidenceContent`, which are consumed by the lab UI.
