# Manual workflow commands for end-to-end run

**Confidence:** 0.76

**Sources:**
- README.md (Manual workflow section)

**Summary:**
The README documents a manual CLI flow that initializes a window+experiment with config fields (including rubric_model_id and scoring_model_id), then creates a run and queues rubric and scoring generation. It shows the exact commands and JSON payload structure needed for an end-to-end run.
