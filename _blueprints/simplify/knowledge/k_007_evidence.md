# README documents sample_count/evidence_cap as experiment config axes

**Confidence:** 0.68

**Sources:**
- README.md

**Summary:**
The design-space table and CLI examples document `config.scoring_stage.sample_count` and `evidence_cap` as experiment config axes, implying the current public contract treats counts as part of experiment configuration.
