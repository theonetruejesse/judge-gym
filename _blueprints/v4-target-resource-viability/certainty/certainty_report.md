# Certainty Report

## Evidence Scores

- `k_001`: `0.84` because the social-science area is well grounded, but top-target bundle contents remain uninspected.
- `k_002`: `0.85` because the comparator area is well supported, with the cleanest evidence around `Zheng` and somewhat lower artifact visibility for `Thakur`.
- `k_003`: `0.86` because `Santurkar` appears materially reconstructable and the main uncertainty is bundle completeness.
- `k_004`: `0.88` because the engine-fit criteria are stable and directly actionable.

## Hypothesis Scores

- `h_A_social_001`: `0.86` because `Gilardi` looks primary-ready, but not yet bundle-verified.
- `h_A_social_002`: `0.74` because `Ziems` is only defensible under explicit subset scope.
- `h_A_comp_001`: `0.91` because `Zheng` has the strongest public comparator surface.
- `h_A_comp_002`: `0.81` because `Thakur` is viable but slightly less directly packaged.
- `h_A_exp_001`: `0.82` because `Santurkar` is resource-real but high-lift.
- `h_A_social_003`: `0.68` because `Törnberg` remains fairness-conditional despite stronger-than-expected resources.

## Step Scores

- `S1`: `0.72` because the launch shape is clear but not fully locked.
- `S2`: `0.95` because direct bundle inspection is the highest-value next step.
- `S3`: `0.92` because `Ziems` needs precommitted subset selection to stay defensible.
- `S4`: `0.87` because choosing between `Zheng` and `Thakur` is a real but manageable fork.
- `S5`: `0.90` because sequencing `Santurkar` later is strongly justified by engine-lift cost.
